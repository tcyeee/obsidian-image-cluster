# TODO: 瀑布流显示模式

## 背景

用户反馈（[issue #1](https://github.com/tcyeee/obsidian-image-cluster/issues/1)）：图片组画廊按固定正方形网格排列，非正方形图片会被裁剪，无法看到原图完整内容。

评估结论：单纯把 CSS 从 `object-fit: cover` 换成 `contain` 代价最低，但非正方形图片会在固定方格里露出空白（letterbox），既不整齐也不完整。更合理的方案是新增"瀑布流"（masonry）布局作为可选显示模式，与现有"有序网格"模式并存，用户按需切换——网格模式保留现状的整齐美观，瀑布流模式保证图片不被裁剪。

关键前提发现：裁剪不止发生在 CSS 层。`src/thumbnail/thumbnail.ts` 的 `ensureThumbnailForFile` 会用 canvas 把原图预先**居中裁剪成正方形**再写入 vault 缓存（`assets/cache/` 下的 JPG），页面显示用的是这份已经被裁过的缓存文件，不是原图。因此仅改 CSS/渲染层不够，缓存生成逻辑也必须支持"不裁剪、等比缩放"的产出，否则瀑布流模式下拿到的还是方形裁剪图。

三个任务有依赖顺序：1（入口）→ 2（缓存支持两种产出）→ 3（真正的瀑布流渲染，依赖 2 提供的非裁剪缩略图）。

---

## 任务 1：图片组菜单新增显示模式切换

**目标**：在图片组的设置面板中新增"有序网格 / 瀑布流"切换项，与现有"画布尺寸 S/M/L"并列。

**涉及文件**：
- `src/render/elements.ts`（约 30-48、79-90 行）：现有 S/M/L 单选组的实现，是新增"网格/瀑布流"单选组的直接模板。
- `src/core/domain.ts`（`SettingOptions` 类）：新增 `layout: "grid" | "masonry"` 字段。
- `src/markdown/style-options.ts`：为配置行解析新增 `layout=` key 的分支，并在 `toConfigLine()` 里补充对应输出。
- `src/settings.ts`（`ImgRowPluginSettings`）：视需要新增全局默认值（新建图片组时的默认显示模式）。

**验收标准**：
- 打开任意图片组设置面板，能看到并切换"有序网格/瀑布流"选项。
- 切换后写入 markdown 代码块的配置行里能看到 `layout=masonry`（或等效字段），重新打开笔记后状态保留。
- 本任务只做开关本身，瀑布流模式下暂时允许渲染效果与网格模式一致（渲染逻辑由任务 3 接管）。

---

## 任务 2：缓存缩略图支持两种显示模式

**目标**：缩略图生成逻辑同时支持"居中裁剪成正方形"（现状，供网格模式使用）和"保留长宽比等比缩放"（供瀑布流模式使用）两种产出，且不会相互覆盖或误用旧缓存。

**涉及文件**：
- `src/thumbnail/thumbnail.ts`：
  - `getThumbPath()`（约 29-31 行）：缓存路径目前由 `文件路径 + 目标分辨率` 的 md5 决定，需要把"显示模式"也编码进 hash 输入，否则切换模式后会复用或覆盖另一模式的缓存。
  - `ensureThumbnailForFile()`（约 40-171 行）：裁剪逻辑写死在第 104-126 行（`cropSize = Math.min(content.sw, content.sh)` 居中裁剪成正方形）。需要新增一个分支：瀑布流模式下不裁剪，只按长边或宽度等比缩放（具体缩放基准需要结合任务 3 的列宽定义来定，见"待定问题"）。
  - `pruneOrphanedThumbnailCache()`：孤儿缓存清理是按"枚举原图 + 当前分辨率重新计算合法路径集合"实现的，加入 mode 维度后此处的合法路径计算需要同步更新（否则另一模式的有效缓存会被误删）。

**验收标准**：
- 网格模式缩略图行为与现状完全一致（不裁剪逻辑不影响现有输出，避免已有用户缓存全部失效重新生成）。
- 瀑布流模式下生成的缩略图保留原图长宽比，不裁剪内容。
- 两种模式的缓存文件互不覆盖，各自独立存在于 `assets/cache/` 下。
- 手动清理孤儿缓存命令不会误删仍然有效的另一模式缓存。

---

## 任务 3：实现图片组的瀑布流布局

**目标**：图片组在"瀑布流"模式下，按每张图片的原始长宽比排列成多列瀑布流，不裁剪、不留白。

**涉及文件**：
- `styles.css`：网格模式的 `.plugin-image-wrapper`（固定方形 + `overflow: hidden`）、`.plugin-image`（`object-fit: cover`）、`.plugin-image-container`（`display: flex; flex-wrap: wrap`）都是均匀方格假设，瀑布流需要新增一套样式（多列布局，可考虑 CSS `column-count` 或 JS 显式定位两种技术方案，需要评估各自与"+N 折叠"的兼容性）。
- `src/render/layout.ts`：
  - `applySettingsToContainer()` / `applySizeAndRadius()`：目前用单一全局 `--plugin-image-size` 决定所有格子的边长，瀑布流下每张图的高度需要按"列宽 ÷ 原图长宽比"单独计算，需要读取图片的原始尺寸信息。
  - `applyLimitRows()`（约 107-156 行）：按 `offsetTop` 做整行折叠的逻辑假设所有行等高，瀑布流下需要替换为讨论中确定的方案——按高度阈值折叠：
    - 高度阈值沿用现有 `limit`（行数）设置，复用网格模式下"N 行 = N × (图片尺寸 + 间距)"的公式换算成像素高度，保证用户从网格切到瀑布流时"显示多少内容"的直觉不突变。
    - 每一列的高度独立累加判断：某一列内一旦下一张图会导致该列高度超过阈值，这张及之后排入该列的图都归入"+N"折叠部分，不同列的截断位置允许不完全对齐（瀑布流的正常表现）。

**验收标准**：
- 瀑布流模式下，非正方形图片完整显示，无裁剪、无明显空白。
- 折叠行为（"+N" 提示）在瀑布流模式下正常工作，展开后能看到全部图片。
- 拖拽排序、点击预览、删除等现有图片组交互在瀑布流模式下依然可用。

---

## 待定问题

- **"+N" 徽标叠加位置**：网格模式下因整行对齐，"+N" 稳定落在最后一行最后一格；瀑布流各列高度不同，需要定一个规则（例如固定叠加在被截断的最短列的最后一张图上，或统一放在容器底部单独一行）。
- **瀑布流缩略图的缩放基准**：是按"固定列宽，动态计算高度"，还是别的策略？需要先定下列宽的取值方式（复用 S/M/L 尺寸设置作为列宽，还是新增独立的列数/列宽设置），任务 2 的缩略图生成基准才能确定。
- **技术选型**：瀑布流用 CSS `column-count`（简单但图片顺序是先列后行，跟拖拽排序的直觉顺序可能不一致）还是 JS 显式计算每列位置（可以严格保持"先行后列"的阅读顺序，但实现复杂度更高）。
- **旧缓存兼容**：现有用户已经生成了大量方形裁剪缓存，任务 2 上线后这批缓存该如何处理（保留供网格模式继续使用，还是提供一次性迁移/重新生成入口）。
- **默认模式**：新建图片组默认使用网格还是瀑布流；已有笔记里没有 `layout=` 字段的旧配置行如何兜底（建议默认网格，保持向后兼容的视觉效果不突变）。
